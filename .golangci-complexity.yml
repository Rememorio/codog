version: "2"

run:
  timeout: 10m

linters:
  default: none
  enable:
    - gocognit
    - gocyclo
  settings:
    gocognit:
      min-complexity: 30
    gocyclo:
      min-complexity: 30

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
